rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow read, write for all collections in the application
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

